#!/usr/bin/perl
# testingwaters.pl
# to test out different perl scripts and see what I can pull out
use strict; use warnings;

my ($x, $y) = @ARGV;  # add numbers in terminal that you want to process

my ($a, $b) = (3, 5);
my ($c, $d) = @ARGV;

print "first number = $c, second number = $d\n";

# no numbers set in terminal, thus error will occur



